// constant variable
const defaultResult = 0;

// variable
let currentResult = defaultResult;
currentResult = (currentResult + 10) * 3 / 2 -1;

let calcDes = `(${currentResult} + 10) * 3 / 2 -1`;

outputResult(currentResult, calcDes);
